[package]
name=arg
version=1.0.0
include=libarg
description=Easier argument and option parsing for shell scripts.
license=MIT

[dependencies]
assign=*
is=*

[devDependencies]
embed-docs=*
unittest=*

[scripts]
readme=embed-docs README.md libis
test=unittest test/*.test
